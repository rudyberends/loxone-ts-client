import { ControlHandle } from '../ControlHandle.js';

/** Shortcut to another Miniserver (local, remote or Trust link). (generated). */
export class MsShortcutControl extends ControlHandle {
  static readonly controlType = 'MsShortcut';
}
