import { ControlHandle } from '../ControlHandle.js';

/** Displays a system scheme image with positioned references to other controls. (generated). */
export class SystemSchemeControl extends ControlHandle {
  static readonly controlType = 'SystemScheme';
}
